version: '3'
services:
  mydb:
    image: postgres
    environment:
      POSTGRES_PASSWORD: admin
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - 5432:5432 
volumes:
  db-data: {}
